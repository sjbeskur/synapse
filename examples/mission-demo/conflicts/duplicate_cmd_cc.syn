namespace camera_app

/// Intentionally reuses camera_app::CameraCommands function code 1.
commands CameraCommands {
    @cc(1)
    command SetRadioMode {
        /// Requested radio mode.
        mode: u8
    }
}
