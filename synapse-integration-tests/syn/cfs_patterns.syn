namespace nav_app

table NavConfig {
    max_speed: f64
    enabled: bool
    frame_id: string[<=32]
}

commands NavCommands {
    @cc(1)
    command SetMode {
        mode: u8
    }
}

telemetry NavState {
    x: f64
    y: f64
}
